import HBoxPanel from '../../../layouts/HBoxPanel/HBoxPanel';
import css from './AuthCard.module.css';

export const AuthCard = () => {
    return (
        <HBoxPanel className={`${css.card}`}>
            
        </HBoxPanel>
    );
};